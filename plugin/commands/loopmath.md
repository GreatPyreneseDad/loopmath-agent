---
description: Set up loopmath cost metering for this project and report findings
---

Follow the `loopmath` skill end to end for this repository: check status,
start the agent if needed, wire this project's model calls through the proxy
(edit the project's env/config, not just the shell), run or ask me to run the
workload, then read findings and propose concrete diffs with the estimated
savings. $ARGUMENTS
